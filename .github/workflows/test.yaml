name: Test

on:
  pull_request:
    paths:
      - src/*.rs
      - Cargo.toml
      - jakefile.toml

env:
  CARGO_TERM_COLOR: always

jobs:
  test:
    name: Test on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Jake (also sets up Rust)
        uses: AstraBert/setup-jake@v0.1.0

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Run tests
        run: jake test

  test-windows:
    name: Test on windows-latest
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Tesseract
        if: runner.os == 'Windows'
        run: choco install tesseract --no-progress
        shell: pwsh

      - name: Set Tesseract env
        if: runner.os == 'Windows'
        run: |
          echo "TESSERACT_INCLUDE_DIRS=C:\Program Files\Tesseract-OCR\include" >> $GITHUB_ENV
          echo "TESSERACT_LIB_DIRS=C:\Program Files\Tesseract-OCR\lib" >> $GITHUB_ENV
        shell: bash

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ${{ runner.os == 'Windows' && '~\\.cargo\\registry' || '~/.cargo/registry' }}
            ${{ runner.os == 'Windows' && '~\\.cargo\\git' || '~/.cargo/git' }}
            target
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Run tests
        run: cargo +nightly test --workspace
